module top_module(
    input [31:0] a,
    input [31:0] b,
    input sub,
    output [31:0] sum
);
    wire x;
    wire [31:0] y;
    
    assign y[31:0] = b[31:0]^{32{sub}};
    
    add16 instance1 (.a(a[15:0]), .b(y[15:0]), .cin(sub), .cout(x), .sum(sum[15:0]));
    add16 instance2 (.a(a[31:16]), .b(y[31:16]), .cin(x), .sum(sum[31:16]));
  
endmodule
