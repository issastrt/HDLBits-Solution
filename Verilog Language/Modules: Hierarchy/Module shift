module top_module ( input clk, input d, output q );
    wire a,b;
    my_dff instance1(.clk(clk), .d(d), .q(a));
    my_dff instance2(.clk(clk), .d(a), .q(b));
    my_dff instance3(.clk(clk), .d(b), .q(q));

endmodule
